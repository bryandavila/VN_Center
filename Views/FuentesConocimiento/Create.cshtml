@model VN_Center.Models.Entities.FuentesConocimiento

@{
    ViewData["Title"] = "Crear Nueva Fuente de Conocimiento";
}

<div class="container-xxl flex-grow-1 container-p-y">

    <h4 class="fw-bold py-3 mb-4">
        <span class="text-muted fw-light">Administración / Fuentes de Conocimiento /</span> Crear Nueva Fuente
    </h4>

    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="card mb-4">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">Información de la Fuente</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Create" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="NombreFuente" class="form-label"></label>
                            <input asp-for="NombreFuente" class="form-control" placeholder="Ej: Instagram, Referencia Universitaria, Búsqueda Online" />
                            <span asp-validation-for="NombreFuente" class="text-danger"></span>
                        </div>

                        <div class="mt-4">
                            <button type="submit" class="btn btn-primary me-2">Crear Fuente</button>
                            <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
