@model VN_Center.Models.Entities.ProgramaProyectoComunidades

@{
    ViewData["Title"] = "Asignar Programa/Proyecto a Comunidad";
}

<div class="container-xxl flex-grow-1 container-p-y">

    <h4 class="fw-bold py-3 mb-4">
        <span class="text-muted fw-light">Gestión de Datos / Programas en Comunidades /</span> Nueva Asignación
    </h4>

    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card mb-4">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">Seleccionar Programa/Proyecto y Comunidad</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Create" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        @if (!ViewData.ModelState.IsValid && ViewData.ModelState[string.Empty] != null)
                        {
                            <div class="alert alert-danger">
                                @foreach (var error in ViewData.ModelState[string.Empty].Errors)
                                {
                                    <span>@error.ErrorMessage</span>

                                    <br />
                                }
                            </div>
                        }

                        <div class="mb-3">
                            <label asp-for="ProgramaProyectoID" class="form-label"></label>
                            <select asp-for="ProgramaProyectoID" class="form-select" asp-items="ViewBag.ProgramaProyectoID">
                                <option value="">-- Seleccione un Programa/Proyecto --</option>
                            </select>
                            <span asp-validation-for="ProgramaProyectoID" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="ComunidadID" class="form-label"></label>
                            <select asp-for="ComunidadID" class="form-select" asp-items="ViewBag.ComunidadID">
                                <option value="">-- Seleccione una Comunidad --</option>
                            </select>
                            <span asp-validation-for="ComunidadID" class="text-danger"></span>
                        </div>

                        <div class="mt-4">
                            <button type="submit" class="btn btn-primary me-2">Asignar a Comunidad</button>
                            <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
