@model VN_Center.Models.Entities.NivelesIdioma

@{
    ViewData["Title"] = "Crear Nuevo Nivel de Idioma";
}

<div class="container-xxl flex-grow-1 container-p-y">

    <h4 class="fw-bold py-3 mb-4">
        <span class="text-muted fw-light">Administración / Niveles de Idioma /</span> Crear Nuevo Nivel
    </h4>

    <div class="row">
        <div class="col-md-6 offset-md-3">
            @* Formulario más pequeño y centrado *@
            <div class="card mb-4">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">Información del Nivel de Idioma</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Create" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="NombreNivel" class="form-label"></label>
                            <input asp-for="NombreNivel" class="form-control" placeholder="Ej: Principiante, Intermedio, Avanzado" />
                            <span asp-validation-for="NombreNivel" class="text-danger"></span>
                        </div>

                        <div class="mt-4">
                            <button type="submit" class="btn btn-primary me-2">Crear Nivel</button>
                            <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
